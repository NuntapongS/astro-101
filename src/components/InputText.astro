---
const { type, placeholder, id, dataTestId } = Astro.props;
---

<input
  type={type}
  id={id}
  class="w-full p-2 mt-2 text-lg text-left text-gray-600 border-b-2 rounded mb-2"
  placeholder={placeholder}
  data-testid={dataTestId}
/>
